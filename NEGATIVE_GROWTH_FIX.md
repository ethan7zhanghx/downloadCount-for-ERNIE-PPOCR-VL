# 负增长问题修复说明

## 问题描述

在"📋 各平台模型下载量详情 (总/周增)"表格中，出现了很多**负增长**的情况。例如：

| 模型 | 平台 | 上周 | 本周 | 增长 |
|------|------|------|------|------|
| ERNIE-4.5-300B-A47B-FP8-Paddle | 其他 | 14 | 8 | **-6** ❌ |
| ERNIE-4.5-VL-28B-A3B-PT | 其他 | 19 | 8 | **-11** ❌ |
| ERNIE-4.5-0.3B-PT | 其他 | 25 | 15 | **-10** ❌ |

这显然不合理，因为下载量只增不减。

## 问题���源

通过深入分析，发现问题来源于**数据库中模型名称格式不一致**：

### 案例分析：ERNIE-4.5-300B-A47B-FP8-Paddle

#### Gitee 平台的历史数据

**2025-10-31 的数据**：
```
repo: Gitee
model_name: ERNIE-4.5-300B-A47B-FP8-Paddle   ← 无 publisher 前缀
publisher: paddlepaddle
download_count: 6
```

**2025-11-07 的数据**：
```
repo: Gitee
model_name: paddlepaddle/ERNIE-4.5-300B-A47B-FP8-Paddle   ← 有 publisher 前缀
publisher: paddlepaddle
download_count: 没有这条记录（因为名称变了，被当作新模型）
```

### 导致的问题

1. **10-31**: Gitee 有该模型数据（6 下载）
2. **11-07**: 因为模型名称变成了 `paddlepaddle/ERNIE-4.5-300B-A47B-FP8-Paddle`，系统认为是新模型
3. **结果**:
   - "其他"平台（包含 Gitee）在 10-31 的总下载量包含了这 6 次
   - "其他"平台在 11-07 的总下载量**不包含**这 6 次（因为名称不匹配）
   - **计算增长**: 8 (本周) - 14 (上周) = -6 ❌

### 为什么会出现名称不一致？

在 `fetchers.py` 的 `fetch_gitee_data` 函数中（572-574行），有代码移除 publisher 前缀：

```python
# 移除 model_name 中的 publisher 前缀
if model_name.startswith(publisher + '/'):
    model_name = model_name.split('/', 1)[1]
```

但是：
1. 这段代码可能是**后来添加的**，早期数据没有执行
2. 或者在某些情况下**执行失败**
3. 导致数据库中同一个模型有两种名称格式

## 解决方案

在 `analysis.py` 中添加**数据标准化处理**，在分析前统一模型名称格式：

### 1. 新增 `normalize_model_names` 函数 (analysis.py:105-128)

```python
def normalize_model_names(data):
    """
    标准化模型名称：移除 model_name 中的 publisher 前缀

    例如：'paddlepaddle/ERNIE-4.5-0.3B-PT' -> 'ERNIE-4.5-0.3B-PT'

    这确保了即使数据库中存储的模型名称格式不一致，
    在分析时也能正确匹配和比较。
    """
    data = data.copy()

    def remove_publisher_prefix(row):
        model_name = str(row['model_name'])
        publisher = str(row['publisher'])

        # 如果模型名称以 "publisher/" 开头，移除前缀
        if publisher and publisher != 'nan' and '/' in model_name:
            if model_name.startswith(publisher + '/'):
                return model_name.split('/', 1)[1]

        return model_name

    data['model_name'] = data.apply(remove_publisher_prefix, axis=1)
    return data
```

### 2. 在 `calculate_weekly_report` 中调用标准化 (analysis.py:255-257)

```python
# 🔴 重要：标准化模型名称（移除 publisher 前缀）
# 这确保了即使数据库中存储的格式不一致，也能正确匹配
data = normalize_model_names(data)
```

## 修复效果

### 修复前
```
🔍 负增长分析:
================================================================================

其他 平台负增长模型:
  ERNIE-4.5-300B-A47B-FP8-Paddle:
    上周: 14 | 本周: 8 | 增长: -6 ❌
  ERNIE-4.5-VL-28B-A3B-Paddle:
    上周: 37 | 本周: 28 | 增长: -9 ❌
  ERNIE-4.5-VL-28B-A3B-PT:
    上周: 19 | 本周: 8 | 增长: -11 ❌
  ... (共8个负增长)
```

### 修复后
```
🔍 负增长分析:
================================================================================

(无负增长模型) ✅
```

## 修改文件

- `analysis.py:105-128` - 新增 `normalize_model_names` 函数
- `analysis.py:255-257` - 在 `calculate_weekly_report` 中调用标准化函数

## 相关问题

这个问题也解释了第一个修复（Publisher 区分）的重要性：

1. **第一个问题**：不同 publisher 的同名衍生模型被统计到一起
2. **第二个问题**：同一个模型因为名称格式不一致，被当作不同模型

两个问题都是**数据一致性**的问题，现在都已修复！

## 后续建议

1. **数据清理**：考虑在数据库层面统一历史数据的模型名称格式
2. **数据验证**：在数据入库前添加验证，确保模型名称格式统一
3. **监控机制**：定期检查是否有负增长出现，作为数据质量的监控指标

---

修复时间: 2025-11-10
修复人: Claude Code
